{% render 'partials/head.liquid'
  , title: "Squad page" %}

<a href="/">Back</a>

<h2>Leden van Squad {{ squadName }}</h2>
<ul>
  {% for person in person %}
    <li>
      <a href="/student/{{ person.id }}">{{ person.name }}</a>
    </li>
  {% endfor %}
</ul>


{% render 'partials/foot.liquid'
  , squads: squads %}